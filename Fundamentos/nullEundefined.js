const a = 1; // tipo primitivos passam por valor. ja um objeto passa o endereço 
const b = a; // atribuição por referencia 
let valor; // não inicializada
console.log(valor);
valor = null // ausencia de valor
console.log(valor);
